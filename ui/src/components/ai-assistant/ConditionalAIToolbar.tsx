'use client';

import React from 'react';
import { useAppSelector } from '@/store';
import AIContextToolbar from './AIContextToolbar';

export default function ConditionalAIToolbar() {
  // AI Assistant has been consolidated into the sidebar as "Sirsi"
  // The floating AI assistant is disabled to avoid duplication
  return null;
}
